package nanosome.notify.field.expr.value {
	import nanosome.notify.field.expr.value.StaticNumber;

	/**
	 * @author Martin Heidegger mh@leichtgewicht.at
	 */
	public const ZERO: StaticNumber = StaticNumber.forValue( 0.0 );
}
