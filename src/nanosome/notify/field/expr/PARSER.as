// @license@ 
package nanosome.notify.field.expr {
	
	/**
	 * Holds the default <code>ExpressionParser</code> to be used by 
	 * <code>Expression</code> to parse input.
	 * 
	 * @author Martin Heidegger mh@leichtgewicht.at
	 * @version 1.0
	 */
	public const PARSER: ExpressionParser = new ExpressionParser();
}
