package nanosome.notify.connect {
	/**
	 * @author Martin Heidegger mh@leichtgewicht.at
	 */
	public function disconnectEntirely( object: * ): Boolean {
		return CONNECTIONS.disconnectEntirely( object );
	}
}
