package nanosome.notify.connect {
	import nanosome.notify.connect.impl.ConnectionRegistry;
	
	/**
	 * @author Martin Heidegger mh@leichtgewicht.at
	 */
	public const CONNECTIONS: ConnectionRegistry = new ConnectionRegistry();
}
